fn main() {
    // Define a function to demonstrate Rust programming concepts
    fn print_message(message: &str) {
        println!("{}", message);
    }

    // Call the function with a string argument
    print_message("Hello, world!");
}
